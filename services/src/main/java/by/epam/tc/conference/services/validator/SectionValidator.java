package by.epam.tc.conference.services.validator;

public class SectionValidator implements Validator<SectionValidator> {
    @Override
    public boolean validate(SectionValidator object) {
        return false;
    }
}
